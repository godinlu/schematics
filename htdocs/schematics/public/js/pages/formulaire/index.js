/**
 * @typedef {import('./formulaire_app.js')}
 */ 

/**
 * 
 */
document.addEventListener("DOMContentLoaded", () =>{
    // init accordion
    init_accordion();

    let app = new FormulaireApp();
});

